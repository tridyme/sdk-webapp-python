services:
  frontend:
    build:
      context: ./frontend
    ports:
      - '80:80'

  backend:
    build:
      context: ./backend
    ports:
      - '8000:8000'
    environment:
      - PYTHONUNBUFFERED=1
